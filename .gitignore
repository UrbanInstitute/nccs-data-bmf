# R artifacts
.Rproj.user
.Rhistory
.RData
.RDataTmp*
.Ruserdata
*.Rproj

# Credentials
C:/Users/tpoongundranar/.boxr-oauth

# Quarto build output (rendered by GitHub Actions)
docs/GUIDEBOOK/

# Data directories (large files, generated outputs)
data/raw/
data/processed/
data/intermediate/
data/checkpoints/
data/quality/
data/geocoding/
data/test/
data/validate/

# Output files
*.csv
!data/lookup/*.csv
!data/dictionaries/*.csv
!data/crosswalks/*.csv
!data/crosswalks/legacy_dictionaries_raw/*.csv

# Temp and misc
*.png
*.parquet
# ...but the published county FIPS crosswalk is a committed artifact
# (its _county_points_cache.parquet sibling stays ignored — it is a
# rebuildable single-read snapshot from the geocoded master on S3).
!data/crosswalks/county_fips_crosswalk.parquet

# Publish-time staging manifests (the canonical copy lives on S3; built_at churns)
_manifest.json
